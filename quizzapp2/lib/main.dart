import 'package:flutter/material.dart';
import 'package:quizzapp2/quiz.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Quiz();
  }
}
